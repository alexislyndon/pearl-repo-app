<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Colleagues?</title>
    <link rel="stylesheet" href="styles.css" />
    <link rel="stylesheet" href="modal.css" />
    <link rel="stylesheet" href="sidepanel.css" />
    <link rel="stylesheet" href="tabs.css" />
    <link href="jquery-ui.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Urbanist&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Urbanist&display=swap" rel="stylesheet">
    <style>
      body{
        font-family: "Trebuchet MS", sans-serif;
        margin: 50px;
      }
      .demoHeaders {
        margin-top: 2em;
      }
      #dialog-link {
        padding: .4em 1em .4em 20px;
        text-decoration: none;
        position: relative;
      }
      #dialog-link span.ui-icon {
        margin: 0 5px 0 0;
        position: absolute;
        left: .2em;
        top: 50%;
        margin-top: -8px;
      }
      #icons {
        margin: 0;
        padding: 0;
      }
      #icons li {
        margin: 2px;
        position: relative;
        padding: 4px 0;
        cursor: pointer;
        float: left;
        list-style: none;
      }
      #icons span.ui-icon {
        float: left;
        margin: 0 4px;
      }
      .fakewindowcontain .ui-widget-overlay {
        position: absolute;
      }
      select {
        width: 200px;
      }
      </style>
  </head>
  <body>
    <img
      src="Infinity-1s-200px.svg"
      alt=""
      srcset=""
      id="loader"
      style="display: none"
    />
    <div id="app">
      <div id="mySidepanel" class="sidepanel"></div>
      <div id="myModal" class="modal"></div>
      <div>
        <a href="/"
          ><img src="/PearlPay-Logo.png" alt="" style="height: 40px"
        /></a>

        <select name="" id="select-user" style="float: right;">
          <option value="">Select</option>
          <% admins.forEach(e => { %>
            <% if (e.name == 'a') { %>
           <option value="<%= e.unnest %>" selected><%= e.concat %></option>
             
            <% } else { %>
              <option value="<%= e.unnest %>"><%= e.concat %> </option>
            
          <% }}) %>
        </select>
      </div> <br> <br>
      <!-- <h2 style="display: inline">Colleages</h2> -->
      <div class="tabs"><%- include('partials/tabs.ejs', {tabs:tabs} ) %></div>
      <br />
    </span>
    <div id="main" ></div>
    <!-- <div>
      <form id="test" action="/colleagues" method="POST">
        <input type="text" name="tosend" id="tosend" hidden>
        <input id="autocomplete" title="type &quot;a&quot;" >
        <input type="submit" value="go">
      </form>
    </div>
    <ul id="icons" class="ui-widget ui-helper-clearfix">
      <li class="ui-state-default ui-corner-all" title=".ui-icon-plus"><span class="ui-icon ui-icon-plus"></span></li>
    </ul> -->
  </body>
  <script src="jquery.js"></script>
  <script src="jquery-ui.min.js"></script>
  <script src="jquery-hashchange.js"></script>
  <script src="app.js"></script>
  <script src="newColleague.js"></script>
  <script src="view.js"></script>
</html>
